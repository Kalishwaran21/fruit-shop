{
  "name": "fruit-shop-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "mobile": "npm start --prefix mobile"
  }
}
